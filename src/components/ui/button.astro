---
interface Props {
  size?: "md" | "lg";
  block?: boolean;
  style?: "outline" | "primary" | "inverted";
  class?: string;
  [x: string]: any;
}

const {
  size = "md",
  style = "primary",
  block,
  class: className,
  ...rest
} = Astro.props;

const sizes = {
  md: "px-5 py-2.5",
  lg: "px-6 py-3",
};

const styles = {
  outline: "border-2 border-packers-green-900 hover:bg-packers-green-900 text-packers-green-900 hover:text-white",
  primary:
    "bg-packers-green-900 text-white hover:bg-packers-green-800 border-2 border-transparent",
};
---

<button
  {...rest}
  class:list={[
    "rounded-lg text-center transition font-medium focus-visible:ring-2 ring-offset-2 ring-packers-green-200",
    block && "w-full",
    sizes[size],
    styles[style],
    className,
  ]}
  ><slot />
</button>
